function Chai () {
    return(
        <h3>Chai is ready</h3>
    )
}

export default Chai;

//If html is returned then we can keep the name .jsx
//.js works every where but preferebly when only js is used